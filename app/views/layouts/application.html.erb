<!DOCTYPE html>
<html>
  <head>
    <title>MarketingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-white shadow-md">
      <div class="container mx-auto p-4 flex justify-start items-center">
        <div class="text-lg font-semibold mr-8">
          MarketingApp
        </div>
        <div class="flex space-x-4">
          <%= link_to "Index", spend_forecasts_path, class: "text-gray-700 hover:text-gray-900 px-3 py-2 rounded-md text-s font-medium" %>
        </div>
      </div>
    </nav>

    <div class="container mx-auto p-6">
      <%= yield %>
    </div>
  </body>
</html>
